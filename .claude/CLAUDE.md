# AI Assistant Guidelines - Day One Content Operations\n\n**Tech Stack**: Sanity Studio 4.15.0 | Next.js 16.0.0 (App Router) | TypeScript 5.9.3 (strict) | React 19.2.0 | pnpm workspaces | Biome 2.3.6\n\n**Architecture**: Multi-workspace CMS with automatic `sites` field injection, GROQ-based type generation, live preview with Visual Editing, modular page builder system.\n\n---\n\n## TypeScript Standards\n\n### Critical Rules\n1. **No `any` types** - Always define explicit interfaces or types\n2. **No variable shadowing** - Never reuse names of imports, functions, or outer scope variables\n3. **Prefer interfaces for objects** - Use `type` only for unions, primitives, and utilities\n4. **Type assertions require validation** - Add guard clauses when using `as`\n5. **Use generated types** - Import from `@/sanity/types` (auto-generated, never edit manually)\n\n```typescript\n// Type extraction pattern for discriminated unions\ntype HeroBlock = Extract<\n  NonNullable<PAGE_QUERY_SITE_1Result['pageBuilder']>[number],\n  { _type: 'hero' }\n>;\n```\n\n---\n\n## Sanity Studio Patterns\n\n### Schema Definition\n```typescript\nimport { defineType, defineField } from 'sanity';\n\nexport const eventType = defineType({\n  name: 'event',\n  title: 'Event',\n  type: 'document',\n  fields: [\n    defineField({\n      name: 'title',\n      type: 'string',\n      validation: (rule) => rule.required()\n    })\n  ]\n});\n```\n\n### Multi-Site Architecture\nAll documents automatically receive a `sites` field (injected via `apps/studio/schemaTypes/index.ts:10-42`).\n\n```typescript\n// Enable multi-site content\noptions: { crossSite: true }  // Allows multiple site assignments\n\n// Default: single-site content (no crossSite option)\n```\n\n### Custom Input Components\n```typescript\nimport { set, unset, type ArrayOfPrimitivesInputProps } from 'sanity';\n\nexport const CustomInput = (props: ArrayOfPrimitivesInputProps) => {\n  const { value, onChange } = props;\n  \n  const handleChange = useCallback((newValue: string) => {\n    onChange(newValue ? set(newValue) : unset());\n  }, [onChange]);\n\n  return <input value={value || ''} onChange={(e) => handleChange(e.target.value)} />;\n};\n```\n\n### Structure Tool\n```typescript\nimport type { StructureResolver } from 'sanity/structure';\n\nexport const structureSite1: StructureResolver = (S, context) => {\n  return S.list()\n    .id('root')\n    .title('Site 1')\n    .items([\n      S.documentTypeListItem('page').title('Pages'),\n      S.divider(),\n      S.listItem()\n        .title('Upcoming Events')\n        .child(\n          S.documentList()\n            .filter('_type == \"event\" && date >= now()')\n            .defaultOrdering([{ field: 'date', direction: 'asc' }])\n        )\n    ]);\n};\n```\n\n---\n\n## Type Generation Workflow\n\n```\n1. Define schemas in apps/studio/schemaTypes/\n2. Update GROQ queries in apps/web/src/sanity/lib/queries.ts\n3. Run: cd apps/studio && pnpm typegen\n4. Types generated in apps/web/src/sanity/types.ts\n5. Import types in Web components\n```\n\n**Critical**: \n- Never edit `apps/web/src/sanity/types.ts` manually\n- `predev` and `prebuild` hooks auto-run typegen\n- Configuration: `apps/studio/sanity-typegen.json`\n\n---\n\n## GROQ Query Patterns\n\n```typescript\nimport { groq } from 'next-sanity';\n\nexport const EVENT_QUERY = groq`\n  *[_type == \"event\" && slug.current == $slug][0] {\n    _id,\n    title,\n    date,\n    artists[]-> { _id, name, photo },\n    venue-> { _id, name, city }\n  }\n`;\n\n// Site filtering\nconst query = groq`*[_type == \"page\" && $site in sites]`;\n\n// Use parameters for dynamic values\nconst params = { type: 'event', slug: 'my-event' };\n```\n\n**Best Practices**:\n- Project only required fields\n- Use `->` for reference resolution\n- Always use `groq` template tag for typegen\n\n---\n\n## Next.js 16 Patterns\n\n### Caching\n```typescript\n'use cache: private'\n\nimport { cacheLife } from 'next/cache';\n\nexport default async function Page() {\n  cacheLife({ stale: 60, revalidate: 300 });\n  const data = await sanityFetch({ query: MY_QUERY });\n  return <div>{/* render */}</div>;\n}\n```\n\n### Draft Mode\n```typescript\nimport { draftMode } from 'next/headers';\n\nconst perspective = draftMode().isEnabled ? 'previewDrafts' : 'published';\nconst data = await sanityFetch({ query: MY_QUERY, perspective });\n```\n\n---\n\n## Multi-Workspace Configuration\n\n### Workspace Setup\n**File**: `apps/studio/sanity.config.ts:16-73`\n\n```typescript\nconst sharedConfig: SingleWorkspace = { projectId, dataset, schema };\nconst sharedPlugins: PluginOptions[] = [visionTool()];\n\nconst site1Config: WorkspaceOptions = {\n  ...sharedConfig,\n  name: 'site-1',\n  title: 'Site 1',\n  basePath: '/site-1',\n  plugins: [\n    structureTool({ structure: structureSite1 }),\n    presentationTool({ resolve, previewUrl: {...} }),\n    ...sharedPlugins\n  ]\n};\n```\n\n### Site Configuration\n**File**: `apps/studio/lib/SITES.ts` - Single source of truth for site metadata\n\n```typescript\ninterface Site {\n  name: SiteName;\n  title: string;\n  url: string;\n}\n\nexport const SITES: Site[] = [\n  { name: 'site-1', title: 'Site 1', url: 'http://localhost:3334' }\n];\n```\n\n---\n\n## Page Builder System\n\n### Block Definition\n```typescript\n// Blocks are object types, not documents\nexport const heroBlock = defineType({\n  name: 'hero',\n  title: 'Hero Section',\n  type: 'object',\n  fields: [...]\n});\n```\n\n### Block Rendering\n```typescript\nimport type { PAGE_QUERY_SITE_1Result } from '@/sanity/types';\n\nexport function PageBuilder({ content }: { content: PAGE_QUERY_SITE_1Result }) {\n  return (\n    <>\n      {content?.pageBuilder?.map((block) => {\n        switch (block._type) {\n          case 'hero': return <Hero key={block._key} {...block} />;\n          case 'gallery': return <Gallery key={block._key} {...block} />;\n          default: return <div key={block._key}>Unknown block: {block._type}</div>;\n        }\n      })}\n    </>\n  );\n}\n```\n\n---\n\n## Environment Variables\n\n### Studio (`apps/studio/.env.local`)\n```bash\nSANITY_STUDIO_PROJECT_ID=your_project_id\nSANITY_STUDIO_DATASET=production\nSANITY_STUDIO_HOSTNAME=https://your-studio.sanity.studio\nSANITY_STUDIO_FRONTEND_HOST=http://localhost:3000\n```\n\n### Web (`apps/web/.env.local`)\n```bash\nNEXT_PUBLIC_SANITY_PROJECT_ID=your_project_id\nNEXT_PUBLIC_SANITY_DATASET=production\nSANITY_API_READ_TOKEN=your_read_token\nSANITY_STUDIO_URL=http://localhost:3334\nSANITY_REVALIDATE_SECRET=your_webhook_secret\n```\n\n**Critical**: Studio and Web must use matching `PROJECT_ID` and `DATASET`. Validate at startup.\n\n---\n\n## Common Tasks\n\n### Add Document Type\n1. Create schema in `apps/studio/schemaTypes/documents/newType.ts`\n2. Export from `apps/studio/schemaTypes/documents/index.ts`\n3. Run `cd apps/studio && pnpm typegen`\n\n### Add Page Builder Block\n1. Create schema in `apps/studio/schemaTypes/blocks/newBlock.ts`\n2. Export from `apps/studio/schemaTypes/index.ts`\n3. Update GROQ query in `apps/web/src/sanity/lib/queries.ts`\n4. Run `cd apps/studio && pnpm typegen`\n5. Create component in `apps/web/src/app/components/blocks/newBlock.tsx`\n6. Add case to `page-builder.tsx` switch\n\n### Debug Type Issues\n```bash\ncd apps/studio && pnpm typegen\ncd apps/studio && rm -rf .next dist && pnpm build\ncat apps/web/src/sanity/types.ts | grep \"YourQueryName\"\n```\n\n---\n\n## Code Quality\n\n### Pre-commit Checklist\n```bash\npnpm biome                              # Format and lint\ncd apps/studio && tsc --noEmit          # Type check Studio\ncd apps/web && tsc --noEmit             # Type check Web\ncd apps/studio && pnpm typegen          # Ensure types current\n```\n\n### Critical Issues to Avoid\n- Variable shadowing (imports, functions, constants)\n- `any` types without explicit interfaces\n- Type assertions without validation guards\n- Manual edits to generated `types.ts`\n- Skipping `pnpm typegen` after schema changes\n- Mismatched PROJECT_ID between Studio and Web\n- Hardcoded site names (use SITES configuration)\n- Dead code in conditionals\n\n---\n\n## Key Files\n\n**Configuration**:\n- `apps/studio/sanity.config.ts` - Multi-workspace setup\n- `apps/studio/schemaTypes/index.ts:10-42` - Sites field injection\n- `apps/studio/lib/SITES.ts` - Site metadata\n- `apps/studio/sanity-typegen.json` - Type generation config\n\n**Runtime**:\n- `apps/web/src/sanity/lib/queries.ts` - GROQ queries\n- `apps/web/src/sanity/lib/live.ts` - sanityFetch with draft mode\n- `apps/web/src/sanity/types.ts` - Generated types (DO NOT EDIT)\n\n**Patterns**:\n- Schemas: `apps/studio/schemaTypes/documents/*.ts`\n- Blocks: `apps/studio/schemaTypes/blocks/*.ts`\n- Structure: `apps/studio/structure/*.ts`\n- Custom inputs: `apps/studio/schemaTypes/components/*.tsx`\n- Renderers: `apps/web/src/app/components/blocks/*.tsx`\n\n---\n\n**Project**: Day One Content Operations v1.0.0 | **Last Updated**: 2025-11-24